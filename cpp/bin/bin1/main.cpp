// #include <util.h>
#include <exception>
#include <iostream>

#include "hoang.h"

int main() {
  std::cout << "bin1" << std::endl;
  return 0;
}

// void signalHandler(int signal) {
//   std::cout << "\nCaught signal " << signal << ", exiting gracefully!\n";
//   // exit(signal);  // Terminate the program
// }

// int main() {
//   std::cout << "bin1" << std::endl;

//   signal(SIGINT, signalHandler);  // Register signal handler for Ctrl+C

//   std::cout << "Press Ctrl+C to trigger SIGINT...\n";

//   try {
//     throw std::runtime_error("haong");
//     sched_setschedulerds

//   } catch (const std::exception& e) {
//     std::cerr << e.what() << '\n';
//   } catch (...) {
//     std::cerr << "unknown" << '\n';
//   }

//   return 0;
// }

// functionx( intpu ) {
//   if *(intpu) {

//   }
//   else {}

//   ASSERT();
// }

// functionx(10)

// functionx(  ) {
//   try {
//     throw std::runtime_error("haong");
//     sched_setschedulerds

//   } catch (const std::exception& e) {
//     std::cerr << e.what() << '\n';
//   } catch (...) {
//     std::cerr << "unknown" << '\n';
//   }
// }

// nodeJS-- > exception.handler(callback);
// exception.handler(callback);
// exception.handler(callback);
// exception.handler(callback);
// exception.handler(callback);
// exception.handler(callback);
// exception.handler(callback);
// exception.handler(callback);
// exception.handler(unjknow);